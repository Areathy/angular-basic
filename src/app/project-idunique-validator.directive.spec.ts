import { ProjectsddUniqueValidatorDirective } from './project-idunique-validator.directive';

describe('ProjectsddUniqueValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectsddUniqueValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
